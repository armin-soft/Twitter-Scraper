name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Create Release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          ## What's New in this Release
          
          ### 🚀 New Features
          - Advanced Twitter scraping capabilities
          - Auto-update system with seamless installation
          - Enhanced anti-detection technology
          - Modern rich-based terminal interface
          
          ### 🛠 Improvements
          - Better rate limiting algorithms  
          - Improved session management
          - Enhanced error handling
          - Performance optimizations
          
          ### 🔧 Technical Details
          - Python 3.11+ support
          - Selenium WebDriver integration
          - Multi-format export (CSV, JSON)
          - Secure license management
          
          ### 📋 Installation
          1. Download the release archive
          2. Extract to desired location  
          3. Run `python Original_Source/Run.py`
          4. Enter your license key when prompted
          
          ### 💬 Support
          For support, contact ARMIN-SOFT:
          - Website: https://armin-soft.ir/
          - Telegram: @ARMIN_SOFT
          
        draft: false
        prerelease: false